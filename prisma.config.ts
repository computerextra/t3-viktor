// This file was generated by Prisma, and assumes you have installed the following:
// npm install --save-dev prisma dotenv
import "dotenv/config";
import { defineConfig, env } from "prisma/config";

export default defineConfig({
  schema: "prisma-sage-database/schema.prisma",
  migrations: {
    path: "prisma-sage-database/migrations",
  },
  datasource: {
    url: `sqlserver://${env("SAGE_DATABASE_HOST")};database=${env("SAGE_DATABASE_NAME")};user=${env("SAGE_DATABASE_USER")};password=${env("SAGE_DATABASE_PASSWORD")};encrypt=true;trustServerCertificate=true`,
  },
});
